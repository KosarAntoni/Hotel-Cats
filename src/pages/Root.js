import React from 'react';
import MainTemplate from '../templates/MainTemplate';
import Landing from './Landing';

const Root = () => (
  <MainTemplate>
    <Landing />
  </MainTemplate>
);

export default Root;
